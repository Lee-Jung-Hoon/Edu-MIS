<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="talk.dao.TalkMapper">

	<!-- 게시판 목록을 처리하는 쿼리 -->
	<select id="selectReceiveTalkList" parameterType="int" resultType="TalkVO">
		 select tt.content content, to_char(tt.send_date, 'yyyy-mm-dd')as sendDate, 
		        tt.check_yn checkYN, tt.del_yn delYN, tm.mName mName
		   from t_talk tt, t_member tm
		  where tt.receive_member_no = #{no}                       
		  and tm.mNo = tt.receive_member_no and tt.del_yn = 'n' 
	</select>
	
	<select id="selectSendTalkList" parameterType="int" resultType="TalkVO">
		 select tt.content content, to_char(tt.send_date, 'yyyy-mm-dd')as sendDate, 
		        tt.check_yn checkYN, tt.del_yn delYN, tm.mName mName
		   from t_talk tt, t_member tm
		  where tt.send_member_no = #{no}                            
		  and tm.mNo = tt.send_member_no and tt.del_yn = 'n' 
	</select>
</mapper>